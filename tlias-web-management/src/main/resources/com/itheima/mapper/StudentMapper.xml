<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.StudentMapper">
    <insert id="insert">
        insert into student(name, no, gender, phone,id_card, is_college, address, degree, graduation_date,clazz_id, create_time, update_time)
        VALUES (#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update student
        <set>
            name = #{name},
            no = #{no},
            phone = #{phone},
            gender = #{gender},
            degree = #{degree},
            violation_count = #{violationCount},
            violation_score = #{violationScore},
            clazz_id = #{clazzId},
            <if test="idCard != null and idCard != ''">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null">
                is_college = #{isCollege},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deletes">
        delete from student where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="page" resultType="com.itheima.pojo.Student">
        select  s.id, s.name, s.no, s.gender, s.phone, s.degree, s.id_card, s.is_college, s.address, s.graduation_date, s.violation_count, s.violation_score, s.clazz_id, c.name clazzName, s.create_time, s.update_time
            from student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%', #{name}, '%')
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.class_id = #{clazzId}
            </if>
        </where>
    </select>
<!--    //最高学历, 1: 初中, 2: 高中 , 3: 大专 , 4: 本科 , 5: 硕士 , 6: 博士-->
    <select id="getStudent" resultType="java.util.Map">
        select (case
            when degree = 1 then "初中"
            when degree = 2 then "高中"
            when degree = 3 then "大专"
            when degree = 4 then "本科"
            when degree = 5 then "硕士"
            when degree = 6 then "博士" end
            ) name, count(*) value from student group by degree
    </select>
</mapper>